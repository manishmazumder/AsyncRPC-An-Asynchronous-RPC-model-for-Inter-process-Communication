To compile the files, run the command :  make -f Makefile.asyncRPC
To run the project, open a terminal in the same folder and run the command : sudo ./asyncRPC_server
Open another terminal in the same folder and run the command : sudo ./asyncRPC_client localhost "port_number". Here "port_number" should be provided.
